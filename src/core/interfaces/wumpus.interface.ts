/**
 * @interface IRules
 * @copyright vincle
 * @author Leonardo Castillo
 */
export interface IRules {
  // Pozo
  pit: boolean;
  // Oro
  gold: boolean;
  // Brisa
  breeze: boolean;
  // Hedor
  stench: boolean;
  // Wumpus
  wumpus: boolean;
  // Fecha
  arrow: boolean;
}
